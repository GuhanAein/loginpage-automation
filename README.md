# loginpage-automation
The provided code is a Python script that automates the login process on a Wi-Fi portal. It checks if the device is connected to a specific Wi-Fi network (specified by the SSID)
